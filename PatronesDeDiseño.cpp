// SRP = Cada funcion enfocada en una sola linea. 
// OCP = Mejorar el codigo sin romper lo que funciona.
// LSP = crear funciones intercambiables sin romper el codigo.
// ISP = Dividir la logica en partes mas pequeñas para que cada una tenga una responsabilidad clara.
// DIP = Flexibilidad en la implementacion de dependencias, usando interfaces o clases abstractas para desacoplar el codigo.
// RAI = Gestiona recursos de manera segura y automatica, asegurando que se liberen adecuadamente para evitar fugas de memoria o recursos.
// SINGLETON = Garantiza que una clase tenga una sola instancia y proporciona un punto de acceso global a esa instancia.

#include <iostream>
#include <string>
int main() {
  
}